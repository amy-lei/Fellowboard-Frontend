{
  "name": "dashboard",
  "version": "0.1.0",
  "engines": {
    "npm": "6.4.1",
    "node": "10.14.2"
  },
  "scripts": {
    "client": "cd client && npm i && npm start",
    "server": "cd server && npm i && npm run server",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
